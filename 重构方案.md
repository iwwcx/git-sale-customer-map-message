# IM 消息模块重构方案

## 一、现状分析

### 1.1 技术栈
- **框架**: Vue 2.7 + MUI + Webpack 3
- **运行方式**: H5 页面通过 webview 嵌入 uniapp 小程序/App
- **IM SDK**: `@digital-supply-chain/imsdk` (MIMC 协议)
- **文件上传**: `@maidi/file` + 华为云 OBS
- **图片选择**: `weixin-js-sdk` (微信 JSSDK)

### 1.2 消息模块文件清单

| 文件 | 行数 | 职责 | DOM 依赖程度 |
|------|------|------|-------------|
| `chat.vue` | 184 | 聊天主容器，协调 content 和 footer | 中 |
| `chat-content.vue` | 165 | 消息列表，MUI 滚动 | 高（mui scroll） |
| `chat-footer.vue` | 1041 | 输入区，contenteditable + 键盘管理 | 极高 |
| `message-item.vue` | 689 | 单条消息渲染，长按菜单 | 高（DOM 操作） |
| `layer-emotion.vue` | 57 | 表情面板 | 中 |
| `layer-operate.vue` | 332 | 更多操作面板（图片/拍照/视频等） | 高（wx JSSDK） |
| `widget/chat-ico.vue` | 23 | 表情消息组件 | 低 |
| `widget/chat-img.vue` | ~170 | 图片消息组件 | 中 |
| `widget/chat-audio.vue` | ~85 | 语音消息组件 | 中 |
| `widget/chat-file.vue` | ~350 | 文件消息组件 | 中 |
| `widget/chat-data.vue` | ~50 | 数据卡片消息 | 低 |
| `widget/chat-link.vue` | ~90 | 链接消息 | 低 |
| `widget/chat-video.vue` | ~120 | 视频消息 | 中 |
| `widget/chat-at.vue` | ~20 | @提及组件 | 低 |
| `widget/preview.ts` | 61 | 消息内容渲染器（动态组件） | 高（Vue.extend） |

### 1.3 Services 层文件清单

| 文件 | 行数 | 职责 | DOM 依赖 |
|------|------|------|---------|
| `message.ts` | 674 | 消息 CRUD、发送、加载 | 低（少量 window） |
| `im.ts` | 362 | IM 连接、登录、消息接收 | 低 |
| `quote.ts` | 199 | 引用消息解析与渲染 | 中（DOM 操作） |
| `recent.ts` | ~700 | 最近会话列表 | 低 |
| `group.ts` | ~560 | 群组管理 | 低 |
| `shake.ts` | 8 | 窗口抖动 | 无 |

### 1.4 核心痛点

#### 痛点1: contenteditable 输入框（chat-footer.vue 1041行）
- 大量代码处理焦点切换、光标定位、键盘保持（`hiddenInput` + `contenteditable` 双焦点方案）
- `insert()` 方法通过 DOM API 插入表情图片节点
- `paste()` 方法处理粘贴板内容
- `adjustInputHeight()` 手动计算输入框高度
- `setInsertNextSibling()` 管理光标位置
- **在 uniapp 原生中这些全部不需要**

#### 痛点2: MUI 模拟滚动（chat-content.vue）
- `mui($el).absoluteScroll()` 模拟原生滚动
- 手动计算 `scrollTo`、`maxScrollY`、`lastBottom`
- **uniapp `scroll-view` 原生支持，性能更好**

#### 痛点3: 消息渲染（preview.ts + message-item.vue）
- `PreviewMessage()` 使用 `Vue.extend()` 动态创建组件实例
- 通过正则替换消息文本为组件标签，再编译模板
- `message-item.vue` 在 `mounted()` 中手动 `appendChild`
- **uniapp 不支持动态编译模板，需要改为数据驱动渲染**

#### 痛点4: 表情包体积
- 95个表情，GIF 格式总计约 1.2MB
- PNG 缩略图约 500KB
- SVG 约 50KB
- **微信小程序主包限制 2MB，表情包直接超限**

#### 痛点5: weixin-js-sdk 依赖
- `layer-operate.vue` 用 `wx.chooseImage()` 选图
- `message-item.vue` 用 `wx.previewImage()` 预览图片
- **uniapp 有原生 `uni.chooseImage()` / `uni.previewImage()`，不需要 JSSDK**

---

## 二、目标架构

### 2.1 整体架构

```
uniapp 项目 A ─┐
uniapp 项目 B ──┼── 引用 uni_modules/uni-im-chat (私有 Git 仓库)
uniapp 项目 C ─┘
```

### 2.2 uni_modules 目录结构

```
uni_modules/
└── uni-im-chat/
    ├── package.json                    # uni_modules 配置
    ├── readme.md
    ├── components/
    │   ├── uni-im-chat/                # 聊天主容器（入口组件）
    │   │   └── uni-im-chat.vue
    │   ├── uni-im-chat-content/        # 消息列表
    │   │   └── uni-im-chat-content.vue
    │   ├── uni-im-chat-footer/         # 输入区
    │   │   └── uni-im-chat-footer.vue
    │   ├── uni-im-message-item/        # 单条消息
    │   │   └── uni-im-message-item.vue
    │   ├── uni-im-emotion-panel/       # 表情面板
    │   │   └── uni-im-emotion-panel.vue
    │   ├── uni-im-operate-panel/       # 更多操作面板
    │   │   └── uni-im-operate-panel.vue
    │   └── uni-im-message-widgets/     # 消息类型组件
    │       ├── uni-im-msg-text.vue     # 文本消息
    │       ├── uni-im-msg-image.vue    # 图片消息
    │       ├── uni-im-msg-audio.vue    # 语音消息
    │       ├── uni-im-msg-file.vue     # 文件消息
    │       ├── uni-im-msg-video.vue    # 视频消息
    │       ├── uni-im-msg-data.vue     # 数据卡片
    │       ├── uni-im-msg-link.vue     # 链接消息
    │       ├── uni-im-msg-emotion.vue  # 表情消息
    │       ├── uni-im-msg-quote.vue    # 引用消息
    │       └── uni-im-msg-at.vue       # @提及
    ├── libs/                           # 纯逻辑层（无 DOM 依赖）
    │   ├── emotion.ts                  # 表情配置与服务
    │   ├── message-parser.ts           # 消息文本解析器
    │   └── types.ts                    # 类型定义
    ├── services/                       # 业务服务层
    │   ├── message-service.ts          # 消息 CRUD
    │   ├── im-service.ts               # IM 连接管理
    │   ├── quote-service.ts            # 引用消息解析
    │   └── upload-service.ts           # 文件上传
    ├── config/                         # 配置
    │   └── index.ts                    # API 地址、OSS 配置等
    └── static/                         # 静态资源
        └── emotion/
            └── emotion-sprite.svg      # SVG 降级用（~50KB）
```

### 2.3 分层设计

```
┌─────────────────────────────────────────────┐
│           宿主 uniapp 项目                   │
│  (页面、路由、登录、业务 API 配置)             │
├─────────────────────────────────────────────┤
│       uni_modules/uni-im-chat               │
│  ┌─────────────────────────────────────┐    │
│  │         UI 组件层 (Vue 3)            │    │
│  │  chat / content / footer / item      │    │
│  │  emotion-panel / operate-panel       │    │
│  │  message-widgets/*                   │    │
│  ├─────────────────────────────────────┤    │
│  │         服务层 (纯 TS)               │    │
│  │  message-service / im-service        │    │
│  │  quote-service / upload-service      │    │
│  ├─────────────────────────────────────┤    │
│  │         基础层 (纯 TS)               │    │
│  │  emotion / message-parser / types    │    │
│  └─────────────────────────────────────┘    │
├─────────────────────────────────────────────┤
│       uniapp 原生 API                        │
│  uni.chooseImage / uni.previewImage          │
│  uni.createInnerAudioContext / scroll-view   │
└─────────────────────────────────────────────┘
```

### 2.4 组件接口设计

#### 入口组件 `uni-im-chat`

```typescript
// Props
interface UniImChatProps {
  interlocutorKey: string      // 会话标识，如 "1:123" (CategoryId:DataId)
  interlocutorName?: string    // 对话方名称
  interlocutorLogo?: string    // 对话方头像
}

// Events
interface UniImChatEvents {
  (e: 'back'): void                              // 返回
  (e: 'user-info', userId: string): void         // 查看用户信息
  (e: 'group-info', groupId: string): void       // 查看群信息
}

// Slots
interface UniImChatSlots {
  header?: () => any    // 自定义头部导航
  footer?: () => any    // 自定义底部扩展
}
```

---

## 三、表情包方案

### 3.1 方案对比

| 方案 | 包体积 | 动画 | 离线可用 | 实现复杂度 | 推荐 |
|------|--------|------|---------|-----------|------|
| GIF 打包本地 | ~1.2MB | ✅ | ✅ | 低 | ❌ 超限 |
| CDN 远程 GIF | 0 | ✅ | ❌ | 低 | ✅ |
| SVG Sprite 本地 | ~50KB | CSS 动画 | ✅ | 中 | ✅ 降级 |
| Unicode Emoji | 0 | 系统支持 | ✅ | 低 | 风格不统一 |
| 表情字体 woff2 | ~150KB | ❌ | ✅ | 中 | 不推荐 |

### 3.2 推荐方案: CDN 远程加载 + SVG 降级

```
消息传输格式（不变）: [微笑]  →  对应编号 [101]
                         ↓
渲染时根据环境选择:
  ├── 有网络: CDN GIF URL (动图)
  └── 无网络: 本地 SVG Sprite (静态)
```

#### CDN 路径规划

```
https://cdn.global-dsc.cn/im/emotion/
  ├── 0101.gif    # 动图
  ├── 0101.svg    # 静态
  ├── 0102.gif
  ├── 0102.svg
  └── ...
```

#### EmotionService 改造

```typescript
// libs/emotion.ts (新)
const CDN_BASE = 'https://cdn.global-dsc.cn/im/emotion'  // 可配置

export class EmotionService {
  // 获取 GIF 动图 URL
  static gif(code: string): string {
    return `${CDN_BASE}/${code}.gif`
  }

  // 获取 PNG 缩略图 URL（用于输入栏小图）
  static png(code: string): string {
    return `${CDN_BASE}/${code}.png`
  }

  // 获取本地 SVG 降级（打包进 static，约 50KB）
  static svg(code: string): string {
    return `/static/emotion/${code}.svg`
  }

  // 智能选择：优先 CDN，失败回退 SVG
  static getUrl(code: string, animated: boolean = true): string {
    return animated ? this.gif(code) : this.svg(code)
  }
}
```

#### 表情面板组件

```vue
<!-- uni-im-emotion-panel.vue -->
<template>
  <scroll-view scroll-y class="emotion-panel">
    <view class="emotion-grid">
      <view
        v-for="(tip, code) in emotionConfig"
        :key="code"
        class="emotion-item"
        @tap="onSelect(code, tip)"
      >
        <image
          :src="getEmotionUrl(code)"
          mode="aspectFit"
          class="emotion-img"
        />
      </view>
    </view>
  </scroll-view>
</template>
```

### 3.3 表情上传脚本

需要写一个脚本将本地 `src/assets/emotion/` 下的 GIF/SVG 上传到 CDN：

```
scripts/upload-emotion.ts
  1. 读取 src/assets/emotion/ 下所有 .gif 和 .svg 文件
  2. 上传到华为云 OBS（复用现有 obsSend 逻辑）
  3. 输出上传结果和 URL 映射表
```

### 3.4 体积估算

| 内容 | 大小 | 位置 |
|------|------|------|
| SVG 降级文件（95个） | ~50KB | 打包进 static |
| EmotionConfig 映射表 | ~3KB | 代码内 |
| CDN GIF（95个） | ~1.2MB | 远程，不占包 |
| **小程序包内增量** | **~53KB** | ✅ 远低于 2MB |

---

## 四、消息渲染方案

### 4.1 问题

当前 `preview.ts` 使用 `Vue.extend()` 动态编译模板字符串，uniapp 不支持这种方式。需要改为**数据驱动的分段渲染**。

### 4.2 消息解析器设计

将消息文本解析为**段落数组**，每个段落对应一种类型：

```typescript
// libs/message-parser.ts

interface MessageSegment {
  type: 'text' | 'emotion' | 'image' | 'audio' | 'file' | 'video' | 'data' | 'link' | 'at' | 'quote'
  content: string           // 原始内容
  props?: string            // 解析后的属性
  children?: MessageSegment[] // 引用消息的子段落
}

// 解析消息文本为段落数组
function parseMessage(msgText: string, interlocutorKey: string): MessageSegment[] {
  const segments: MessageSegment[] = []

  // 正则匹配所有 <m_xxx,props> 标签
  const regex = /<m_([a-z]+),([^>]+)>/gi
  let lastIndex = 0
  let match: RegExpExecArray | null

  while ((match = regex.exec(msgText)) !== null) {
    // 标签前的纯文本
    if (match.index > lastIndex) {
      segments.push({
        type: 'text',
        content: msgText.slice(lastIndex, match.index)
      })
    }

    const [, tag, props] = match
    // 根据标签类型创建段落
    if (tag === 'ico') {
      segments.push({ type: 'emotion', content: match[0], props })
    } else if (tag === 'img') {
      segments.push({ type: 'image', content: match[0], props })
    } else if (tag === 'audio') {
      segments.push({ type: 'audio', content: match[0], props })
    } else if (tag === 'file') {
      segments.push({ type: 'file', content: match[0], props })
    } else if (tag === 'video') {
      segments.push({ type: 'video', content: match[0], props })
    } else if (tag === 'data') {
      segments.push({ type: 'data', content: match[0], props })
    } else if (tag === 'link') {
      segments.push({ type: 'link', content: match[0], props })
    } else if (tag === 'at') {
      segments.push({ type: 'at', content: match[0], props })
    } else if (tag === 'quote') {
      // 引用消息特殊处理
      const quoteMatch = msgText.match(/<m_quote,([^>]+)>(.*?)<\/m_quote>/)
      if (quoteMatch) {
        segments.push({
          type: 'quote',
          content: match[0],
          props: quoteMatch[1],
          children: parseMessage(quoteMatch[2], interlocutorKey)
        })
      }
    }

    lastIndex = regex.lastIndex
  }

  // 尾部纯文本
  if (lastIndex < msgText.length) {
    segments.push({
      type: 'text',
      content: msgText.slice(lastIndex)
    })
  }

  return segments
}
```

### 4.3 消息渲染组件

```vue
<!-- uni-im-message-item.vue -->
<template>
  <view class="message-item" :class="{ 'message-me': message.IsMe }">
    <!-- 时间分隔 -->
    <view v-if="message.ShowTime" class="message-time">{{ messageTime }}</view>

    <!-- 系统提示 -->
    <view v-if="tip" class="message-tip">{{ tip }}</view>

    <!-- 消息内容 -->
    <view v-else class="message-body">
      <image :src="message.SendUserLogo" class="message-avatar" />
      <view class="message-info">
        <text v-if="!isSingleChat" class="message-sender">{{ message.SendUserName }}</text>

        <view class="message-bubble" @longpress="onLongPress">
          <!-- 遍历渲染消息段落 -->
          <template v-for="(seg, idx) in segments" :key="idx">
            <uni-im-msg-text   v-if="seg.type === 'text'" :text="seg.content" />
            <uni-im-msg-emotion v-else-if="seg.type === 'emotion'" :code="seg.props" />
            <uni-im-msg-image   v-else-if="seg.type === 'image'" :props="seg.props" />
            <uni-im-msg-audio   v-else-if="seg.type === 'audio'" :props="seg.props" :is-me="message.IsMe" />
            <uni-im-msg-file    v-else-if="seg.type === 'file'" :props="seg.props" />
            <uni-im-msg-video   v-else-if="seg.type === 'video'" :props="seg.props" />
            <uni-im-msg-data    v-else-if="seg.type === 'data'" :props="seg.props" />
            <uni-im-msg-link    v-else-if="seg.type === 'link'" :props="seg.props" />
            <uni-im-msg-at      v-else-if="seg.type === 'at'" :props="seg.props" />
            <uni-im-msg-quote   v-else-if="seg.type === 'quote'" :segments="seg.children" :quote-props="seg.props" />
          </template>
        </view>

        <!-- 发送状态 -->
        <view class="message-state">
          <view v-if="message.State === -1" class="loading-spinner" />
          <view v-if="message.State === 0" class="state-warn">!</view>
        </view>
      </view>
    </view>

    <!-- 长按菜单 -->
    <view v-if="menuVisible" class="longtap-menu">
      <view v-for="menu in menus" :key="menu.code" @tap="onMenuTap(menu)">
        {{ menu.label }}
      </view>
    </view>
  </view>
</template>
```

---

## 五、输入区方案

### 5.1 核心变化

| 现状 (H5) | 重构后 (uniapp) |
|-----------|----------------|
| `contenteditable` div | `textarea` + 富文本分段 |
| `hiddenInput` 保持键盘 | 不需要，`adjust-position` 自动处理 |
| 手动 `insert()` DOM 节点 | 在文本中插入 `[微笑]` 标记 |
| 手动 `adjustInputHeight()` | `textarea` 的 `auto-height` 属性 |
| `window.getSelection()` 管理光标 | `textarea` 原生光标 |
| `paste()` 处理粘贴板 | `textarea` 原生粘贴 |
| `KeyboardService` 监听键盘 | `@focus` / `@blur` 事件 |

### 5.2 输入区组件设计

```vue
<!-- uni-im-chat-footer.vue -->
<template>
  <view class="chat-footer">
    <!-- 引用消息预览 -->
    <view v-if="quotedMessage" class="quote-preview">
      <text class="quote-author">{{ quotedMessage.SendUserName }}</text>
      <text class="quote-text">{{ quotePreviewText }}</text>
      <view class="quote-close" @tap="clearQuote">✕</view>
    </view>

    <!-- 输入行 -->
    <view class="input-row">
      <!-- 表情按钮 -->
      <view class="footer-btn" @tap="toggleEmotion">
        <text :class="emotionShow ? 'icon-keyboard' : 'icon-smile'"></text>
      </view>

      <!-- 输入框 -->
      <textarea
        v-model="inputText"
        :auto-height="true"
        :show-confirm-bar="false"
        :adjust-position="true"
        :cursor-spacing="20"
        placeholder="请输入消息"
        class="chat-input"
        :focus="inputFocus"
        @focus="onFocus"
        @blur="onBlur"
        @input="onInput"
      />

      <!-- 更多/发送按钮 -->
      <view v-if="!inputText" class="footer-btn" @tap="toggleOperate">
        <text class="icon-add"></text>
      </view>
      <view v-else class="footer-btn send-btn" @tap="onSend">
        <text class="icon-send">发送</text>
      </view>
    </view>

    <!-- 表情面板 -->
    <uni-im-emotion-panel
      v-if="emotionShow"
      @select="onEmotionSelect"
    />

    <!-- 操作面板 -->
    <uni-im-operate-panel
      v-if="operateShow"
      :interlocutor="interlocutor"
      @send="onOperateSend"
    />
  </view>
</template>
```

### 5.3 表情插入逻辑

```typescript
// 表情选择时，在 textarea 当前光标位置插入 [微笑] 文本
function onEmotionSelect(code: string, tip: string) {
  const text = inputText.value
  const cursor = cursorPosition.value  // 通过 @input 事件记录

  // 在光标位置插入表情标记
  inputText.value =
    text.slice(0, cursor) +
    tip +              // 插入 [微笑]
    text.slice(cursor)

  // 移动光标到插入内容之后
  nextTick(() => {
    cursorPosition.value = cursor + tip.length
  })
}
```

### 5.4 发送逻辑

```typescript
async function onSend() {
  let content = inputText.value.trim()
  if (!content) return

  // 处理引用
  if (quotedMessage.value) {
    const userName = quotedMessage.value.SendUserName.split('(')[0].trim()
    const quoteInfo = `${userName},${encodeURIComponent(quotePreviewText.value)}`
    content = `<m_quote,${quoteInfo}>${content}</m_quote>`
  }

  // 发送
  await MessageService.create_send(props.interlocutorKey, content)

  // 清空输入框
  inputText.value = ''
  clearQuote()
}
```

---

## 六、多项目复用方案

### 6.1 uni_modules 机制

uni_modules 是 DCloud 官方的插件规范：

- **目录约定**: 放在 `uni_modules/` 下，自动被 uniapp 识别
- **easycom**: 组件自动引入，无需手动 import
- **更新方式**: 通过 Git 或 DCloud 插件市场

### 6.2 私有 Git 仓库方案

```
# 1. 创建独立 Git 仓库
git init uni-im-chat
# 推送到内部 Git 服务器

# 2. 各项目通过 submodule 引入
cd uniapp-project-a
git submodule add git@your-git-server:uni-im-chat.git uni_modules/uni-im-chat

# 3. 更新
git submodule update --remote uni_modules/uni-im-chat
```

### 6.3 配置注入

各项目通过配置注入差异（API 地址、OSS 配置等）：

```typescript
// 宿主项目 main.ts
import { setConfig } from '@/uni_modules/uni-im-chat/config'

setConfig({
  apiBase: 'https://api.global-dsc.cn',
  cdnBase: 'https://cdn.global-dsc.cn',
  ossConfig: { ... },
  imSdkConfig: { ... }
})
```

### 6.4 条件编译

uniapp 的条件编译处理平台差异：

```vue
<!-- #ifdef MP-WEIXIN -->
<!-- 微信小程序特有逻辑 -->
<!-- #endif -->

<!-- #ifdef APP-PLUS -->
<!-- App 特有逻辑 -->
<!-- #endif -->

<!-- #ifdef H5 -->
<!-- H5 特有逻辑 -->
<!-- #endif -->
```

---

## 七、实施计划

### 阶段一: 基础层迁移（1-2天）

- [ ] 创建 `uni_modules/uni-im-chat` 目录结构
- [ ] 迁移 `types.ts`（接口定义）
- [ ] 迁移 `emotion.ts`（改为 CDN 方案）
- [ ] 编写 `message-parser.ts`（消息文本解析器）
- [ ] 编写表情上传脚本，将 GIF/SVG 上传到 CDN

### 阶段二: 服务层迁移（2-3天）

- [ ] 迁移 `message-service.ts`（去掉 DOM 依赖）
- [ ] 迁移 `im-service.ts`（适配 uniapp 网络 API）
- [ ] 迁移 `quote-service.ts`（去掉 DOM 操作）
- [ ] 迁移 `upload-service.ts`（用 `uni.uploadFile` 替代）

### 阶段三: UI 组件重写（5-7天）

按依赖顺序：

- [ ] `uni-im-msg-emotion` - 表情消息（最简单，先做验证方案）
- [ ] `uni-im-msg-text` - 文本消息
- [ ] `uni-im-msg-image` - 图片消息
- [ ] `uni-im-msg-audio` - 语音消息
- [ ] `uni-im-msg-file` - 文件消息
- [ ] `uni-im-msg-video` - 视频消息
- [ ] `uni-im-msg-data` - 数据卡片
- [ ] `uni-im-msg-link` - 链接消息
- [ ] `uni-im-msg-at` - @提及
- [ ] `uni-im-msg-quote` - 引用消息
- [ ] `uni-im-emotion-panel` - 表情面板
- [ ] `uni-im-operate-panel` - 操作面板
- [ ] `uni-im-message-item` - 单条消息（组装上面所有组件）
- [ ] `uni-im-chat-content` - 消息列表（scroll-view）
- [ ] `uni-im-chat-footer` - 输入区（textarea 方案）
- [ ] `uni-im-chat` - 主容器（组装 content + footer）

### 阶段四: 集成与测试（2-3天）

- [ ] 在一个 uniapp 项目中集成测试
- [ ] 验证小程序端表情包体积
- [ ] 验证 App 端交互流畅度
- [ ] 验证消息发送/接收/撤回/引用
- [ ] 验证图片/文件/视频发送
- [ ] 性能对比测试

### 阶段五: 多项目接入（1天）

- [ ] 推送到私有 Git 仓库
- [ ] 编写接入文档
- [ ] 在第二个项目验证 submodule 引入

**总预估: 11-16 个工作日**

---

## 八、风险与注意事项

### 8.1 消息格式兼容
- 消息传输格式（`<m_xxx,props>`）**不变**，保证新旧客户端互通
- `message-parser.ts` 需要完整覆盖所有消息类型
- 引用消息的嵌套解析需要充分测试

### 8.2 表情 CDN 依赖
- CDN 不可用时需要优雅降级到 SVG
- 首次加载需要 loading 占位
- 考虑预加载策略（进入聊天页前预加载表情面板的图）

### 8.3 textarea 的局限
- textarea 无法内联显示表情图片，只能显示 `[微笑]` 文本
- 这是 uniapp 原生方案的固有限制，微信原生聊天也是类似处理
- 发送后在消息列表中正常显示表情图片

### 8.4 长按菜单
- uniapp 的 `@longpress` 事件替代 MUI 的 `@longtap`
- 菜单弹出位置需要计算，不能用 DOM `offsetTop`
- 改为使用 `uni.createSelectorQuery()` 获取节点位置

### 8.5 音频播放
- `AudioService` 需要用 `uni.createInnerAudioContext()` 替代
- 录音用 `uni.getRecorderManager()` 替代

### 8.6 @提及功能
- 当前通过路由跳转到成员选择页，回来后通过 `Events.$emit` 插入
- uniapp 中改为 `uni.navigateTo()` + `uni.$emit()` / `uni.$on()`

---

## 九、不迁移的部分

以下内容**保留在宿主项目**，不放入 uni_modules：

- 登录/认证逻辑（各项目不同）
- 路由配置（各项目不同）
- API 基础地址配置
- 用户信息管理
- 通讯录/群组列表等非聊天页面
- 推送/通知配置

uni_modules 只包含**聊天界面本身**，通过 Props 传入会话信息，通过 Events 通知宿主项目跳转。
